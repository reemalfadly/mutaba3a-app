# Python dependencies for Mutaba3a (متابعة)
# Pinned to versions known to work together with python-for-android / buildozer.
kivy==2.3.0
plyer==2.1.0
requests==2.31.0
python-dateutil==2.9.0.post0
pyjnius==1.6.1 ; platform_system == "Linux"
